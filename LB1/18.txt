#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double a = 1.0;
    double b = 0.001;

    // Сірий Андрій ІСД-21
    double y = cbrt((a - b) / (b + a)) * exp(log(cos((a - b) * M_PI / 8.0)) / 0.137);

    cout << "y = " << y << endl;

    return 0;
}